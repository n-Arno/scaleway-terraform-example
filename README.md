scaleway-terraform-example
==========================

This basic manifest demonstrate how to setup multiple instance via count, add them to a private network complete with public gateway and expose an app via loadbalancer.
